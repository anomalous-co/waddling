# @duckdb/node-api ships a prebuilt native binary that pnpm's strict symlink
# layout fails to resolve. Hoist it (and the rest) into a flat node_modules so
# the native addon and Next.js can require() it normally.
node-linker=hoisted
