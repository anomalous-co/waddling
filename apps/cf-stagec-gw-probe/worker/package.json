{
  "name": "cf-stagec-gw-probe-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Stage D gateway probe: the waddling gateway (DuckDB + quack_serve + birdshot) as a private CF Container Durable Object, serving birdshot-gated quack in production RS256 mode, reached via containerFetch.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.12.1",
    "jose": "^6"
  },
  "devDependencies": {
    "wrangler": "^4",
    "@cloudflare/workers-types": "^4"
  }
}
