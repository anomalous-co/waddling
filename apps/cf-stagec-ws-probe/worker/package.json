{
  "name": "cf-stagec-ws-probe-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Stage C workspace data-plane probe: the WorkspaceSandbox DO + Sandbox container (Model B). Durable, encrypted, isolated per-agent DuckDB workspace; the DO mints presigned R2 URLs and never touches the file bytes.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.12.1",
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "wrangler": "^4",
    "@cloudflare/workers-types": "^4"
  }
}
